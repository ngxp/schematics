{
    "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
    "extends": [
        "@schematics/angular"
    ],
    "schematics": {
        "ng-add": {
            "factory": "./ng-add",
            "aliases": [
                "install"
            ],
            "description": "Add ngxp Schematics to the workspace.",
            "schema": "./ng-add/schema.json"
        },
        "component": {
            "factory": "./component",
            "aliases": [
                "c"
            ],
            "description": "Create an Angular component.",
            "schema": "./component/schema.json"
        },
        "library": {
            "factory": "./library",
            "aliases": [
                "lib"
            ],
            "description": "Create an Angular library.",
            "schema": "./library/schema.json"
        },
        "ngrx-setup": {
            "factory": "./ngrx-setup",
            "description": "Add NgRx to a project.",
            "schema": "./ngrx-setup/schema.json"
        },
        "ngrx-setup-install-dependencies": {
            "factory": "./ngrx-setup#installDependencies",
            "description": "Install NgRx dependencies.",
            "schema": "./ngrx-setup/schema.json",
            "private": true
        },
        "ngrx-setup-apply-configuration": {
            "factory": "./ngrx-setup#applyConfiguration",
            "description": "Configure NgRx in a project.",
            "schema": "./ngrx-setup/schema.json",
            "private": true
        },
        "ngrx": {
            "factory": "./ngrx",
            "description": "Add an ngrx feature state to a module.",
            "schema": "./ngrx/schema.json"
        }
    }
}