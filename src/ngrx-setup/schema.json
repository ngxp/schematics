{
    "$schema": "http://json-schema.org/schema",
    "id": "ngxp-schematics-ngrx-setup",
    "type": "object",
    "properties": {
        "module": {
            "type": "string",
            "default": "app.module.ts",
            "description": "The path to NgModule where the feature state will be registered. The host directory will create/use the new state directory."
        },
        "skipRouterStore": {
            "type": "boolean",
            "default": false,
            "description": "Do not add @ngrx/router-store to dependencies in package.json."
        },
        "skipComponentStore": {
            "type": "boolean",
            "default": false,
            "description": "Do not add @ngrx/component-store to dependencies in package.json."
        },
        "skipStoreLogger": {
            "type": "boolean",
            "default": false,
            "description": "Do not add @ngrx-store-logger to dependencies in package.json."
        }
    }
}